services:
  lightning-db-node:
    container_name: 'test-integration-ltng-db-node'
    build:
      context: ../../../../..
      dockerfile: build/docker/lightning-db-node.Dockerfile
    restart: always
    env_file:
      - ../../.tests.integration.ltng.db.env
    ports:
      - '7001:7001'
      - '50051:50051'
      - '50052:50052'
    volumes:
      - lightning-db-node-integration-test-db:/var/lib/lightning-db-node/data

volumes:
  lightning-db-node-integration-test-db:
    driver: local
